# db/ — frozen migration history

These SQL files were applied to production by hand before Supabase CLI adoption.
This directory is **frozen**: do not add, edit, or delete files here.

New schema changes live in `supabase/migrations/`. See `docs/db-migrations.md`
for the full workflow, and how production was baselined against this history.
